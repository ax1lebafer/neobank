import styles from './styles.module.scss';

export const ExchangeRate = () => {
  return (
    <section className={styles.exchange}>
      <article className={styles.exchange__card}></article>
    </section>
  );
};
